{
  "name": "probejs-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "ProbeJS monorepo",
  "scripts": {
    "build": "npm run build -w tss_plugin && npm run build -w vscode-extension",
    "watch": "npm run watch -w tss_plugin & npm run watch -w vscode-extension",
    "build:plugin": "npm run build -w tss_plugin",
    "build:ext": "npm run build -w vscode-extension"
  },
  "workspaces": [
    "tss_plugin",
    "vscode-extension"
  ]
}
